So makemore is a type of neural networj which will generate more of the same type of things uniquely. For example in this experiment, we are using the names.txt dataset which has a lot of names and we are going to generate similar sounding unique and new names. 

Examples of already built out language models:
1. Bigram 
2. MLP 
3. RNN 
4. Transformers 
5. GRU

Main Notes:
1. So under the hood, makemore is a character level language model, it means that makemore treats every single line in the dataset as an example and inside that example, it treats each example as a series of characters.

